# ==========================================================
# Monitor Configuration
# ==========================================================

# Left monitor (Dell 1440p)
monitor = HDMI-A-1, 2560x1440@60, 0x0, 1

# Main monitor (LG 4K, primary display)
monitor = DP-1, 3840x2160@60, 2560x0, 1

# Optional: set DP-1 as primary (focus default workspace)
monitor = DP-1, preferred, 2560x0, 1, vrr, 1

# Fallback (only used if something is unplugged)
monitor = , preferred, auto, 1
